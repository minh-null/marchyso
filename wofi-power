#!/usr/bin/env bash

choice=$(printf "Shutdown\nReboot\nLogout" | wofi --dmenu --prompt "Power")

case "$choice" in
  Shutdown)
    systemctl poweroff
    ;;
  Reboot)
    systemctl reboot
    ;;
  "Logout")
    hyprctl dispatch exit
    ;;
esac
